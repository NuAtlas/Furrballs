{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Benchmark (host)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/builddebug/Benchmark/Benchmark",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "CMake: configure and build (Debug)",
            "miDebuggerPath": "/usr/bin/gdb"
        }
    ]
}
