% Furrballs: Topology-Aware Cache Placement for NUMA Systems
% ATC 2026 Short Paper (6 pages + refs)
% Double-blind submission

\PassOptionsToPackage{protrusion=true,expansion=false,final}{microtype}
\documentclass[sigplan,screen,anonymous,10pt,natbib=false]{acmart}

\usepackage{listings}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{pgf}

% matplotlib PGF backend uses \mathdefault which is not defined in all setups
\providecommand{\mathdefault}[1]{#1}

\setcopyright{none}

% Listings style
\lstset{
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue!70!black},
  commentstyle=\color{green!50!black},
  stringstyle=\color{red!60!black},
  breaklines=true,
  showstringspaces=false,
  frame=none,
  columns=fullflexible,
}

\acmDOI{}
\acmISBN{}
\acmConference[ATC'26]{2026 USENIX Annual Technical Conference}{November 2026}{Santa Clara, CA, USA}
\acmYear{2026}
\copyrightyear{2026}
\acmPrice{}

\begin{document}

\title{Furrballs: Topology-Aware Cache Placement\\for Asymmetric Memory Systems}

\author{Anonymous}
\affiliation{
  \institution{Anonymous}
  \country{}
}

\begin{abstract}
Modern servers have deeply asymmetric memory hierarchies: NUMA nodes with
different access latencies, CXL-attached memory pools, and tiered DRAM/HBM
configurations. Production caches (CacheLib, TBB, RocksDB) treat memory as
uniform, incurring cross-domain latency penalties on every access that misses
the local NUMA node's L3 cache.

We present Furrballs, a topology-aware resource placement library that uses
memory topology as a first-class input to placement and eviction decisions.
Each NUMA node receives its own pre-allocated page pool, independent ARC
(Adaptive Replacement Cache) eviction, and per-node lock partitioning. Keys
are routed to nodes via thread-local placement, eliminating cross-domain
access for read-after-write workloads.

On real NUMA hardware (Intel Xeon Ice Lake, 2 nodes; AMD EPYC Milan,
4 nodes), Furrballs achieves up to 9.5$\times$ lower p50 GET latency than
CacheLib and 100\% memory efficiency versus CacheLib's 50\%. Under YCSB
standard workloads, Furrballs delivers 22$\times$ lower latency and
10$\times$ higher throughput than CacheLib at equal cache capacity. A
per-node statistics sharding fix resolved a 6.6$\times$ thread-scaling
anomaly caused by cache-line-aligned atomics crossing socket boundaries---a
cautionary finding for NUMA-aware systems.

\end{abstract}

\begin{CCSXML}
<ccs2012>
<concept>
<concept_id>10011007.10011074.10011099.10011102.10011103</concept_id>
<concept_desc>Software and its engineering~Main memory</concept_desc>
<concept_significance>500</concept_significance>
</concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Software and its engineering~Main memory}

\keywords{NUMA, cache, topology-aware placement, ARC, memory hierarchy}

\maketitle

% ========================================================================
\section{Introduction}
\label{sec:intro}

Server memory is not uniform. A dual-socket Intel Xeon has two NUMA nodes
with $\sim$100\,ns local access and $\sim$150\,ns cross-socket access.
AMD EPYC with 4 NUMA nodes has three distance tiers: local (10), same-socket
(12), and cross-socket (32) on the Infinity Fabric. CXL-attached memory
adds a fourth tier at $\sim$500\,ns.

Production caches ignore this asymmetry. CacheLib~\cite{cachelib} allocates
from a single pool with no NUMA binding, consuming 50\% more memory than
usable capacity due to slab overhead. RocksDB's block cache uses a sharded
LRU with no topology awareness. Intel TBB's concurrent hash map is unbounded
by design. All three treat memory as a uniform resource.

This paper makes the case for \emph{topology-aware cache placement}: using
memory topology as a first-class input to placement decisions. We contribute:

\begin{enumerate}
\item A page-based cache architecture where pages are pre-allocated on
  specific NUMA nodes via one physical allocation per domain, subdivided
  by a bump allocator.
\item Thread-local key routing that places keys on the requesting thread's
  NUMA node, ensuring local access for read-after-write workloads.
\item Per-node lock partitioning and ARC eviction, eliminating cross-node
  contention on the hot path.
\item Cross-platform evaluation on Intel (2-node) and AMD (4-node) NUMA
  hardware under synthetic and YCSB workloads, demonstrating up to
  $9.5\times$ lower latency than CacheLib at half the memory footprint.
\item A cautionary finding: cache-line-aligned statistics atomics that
  prevent intra-socket false sharing caused a $6.6\times$ cross-socket
  scaling regression, resolved by per-node stat sharding.
\end{enumerate}

% ========================================================================
\section{Architecture}
\label{sec:arch}

\subsection{Per-Node Page Pools}

At initialization, Furrballs allocates one contiguous memory region per NUMA
node using \texttt{numa\_alloc\_onnode}, subdivided into fixed-size pages
(4\,KB). Each page is owned by a single node; no cross-node allocation
occurs. Keys are stored inline in pages via a bump allocator. When a page
fills, the next free page is advanced atomically
(\texttt{std::atomic<size\_t>}). This guarantees that all data for a given
node resides on that node's DRAM.

\subsection{Thread-Local Key Routing}

Each key is hashed to determine its home node. With thread-local routing,
the home node is the requesting thread's current NUMA node
(cached in \texttt{thread\_local} to avoid a syscall per access). A GET first
checks the local node; if missed, it scans other nodes. For
read-after-write workloads (the common case for caches), the local check
always hits.

\subsection{Per-Node CMap and ARC}

Each node has its own CMap (concurrent Swiss table with SSE2 SIMD probing)
and ARC (Adaptive Replacement Cache) eviction policy. The CMap uses per-slot
seqlocks for lock-free reads and CAS for write serialization. ARC maintains
two lists (recent and frequent) per node, with a promotion buffer for
deferred, lock-free promotions.

Per-node partitioning eliminates all cross-node lock contention: each
thread operates on its own node's CMap, SpinLock, and maintenance thread.

\subsection{Per-Node Statistics}

\textbf{(Lesson learned.)} Performance counters were originally stored as
14 \texttt{alignas(64) std::atomic<>} fields in the cache object (heap-allocated
on node~0). Each GET touched 3 cache lines (hit count, bytes read, local hit
count) via \texttt{lock~xadd}. On a single socket, the \texttt{alignas(64)}
isolation prevented false sharing between counters. Across sockets, it
\emph{maximized} cross-socket transfer: 3 exclusive cache-line migrations
per GET at $\sim$120\,ns each on Infinity Fabric, adding $\sim$360\,ns.

The fix: hot-path counters are now per-node atomics in the NUMA-allocated
\texttt{PerNodeDetails}. Each thread writes to its own on-node counters;
a background aggregation drains to the global counters every 2\,ms. This
resolved a 6.6$\times$ 4-thread scaling regression (587\,ns $\to$
71\,ns, matching single-thread baseline).

% ========================================================================
\section{Evaluation}
\label{sec:eval}

\subsection{Methodology}

We evaluate on two EC2 bare-metal instances:
\textbf{c6i.metal} (Intel Xeon Platinum 8375C, Ice Lake, 2 NUMA nodes,
56\,MB L3 per socket) and \textbf{c6a.metal} (AMD EPYC 7R13, Milan, 4 NUMA
nodes, 32\,MB L3 per CCD). Both run Ubuntu with kernel 5.15+.

We compare five systems:
\textbf{FurrBallTL} (thread-local routing),
\textbf{FurrBallSN} (single-node, all threads share one node),
\textbf{CacheLib} (Facebook's production cache, LRU),
\textbf{RocksDB LRU} (standalone \texttt{NewLRUCache}),
and \textbf{TBB} (unbounded concurrent hash map, throughput ceiling).

Benchmarks include synthetic partitioned workloads (Zipfian
$\theta\!=\!0.99$) and YCSB standard workloads A (50R/50W),
B (95R/5W), and C (100R). All systems run on the same instance under
identical conditions. We report p50/p99 latency and throughput.

\subsection{Working-Set Sweep}

\begin{figure}[t]
\centering
\resizebox{\columnwidth}{!}{\input{figures/ycsb_latency_sweep.pgf}}
\caption{YCSB-B p50 GET latency across value sizes (2 threads, 32\,MB cache).
  At 1024B (100\,MB working set vs.\ 32\,MB cache), FurrBallTL achieves
  80\,ns vs.\ CacheLib's 370\,ns. TBB is unbounded (100\% hit rate at all sizes).}
\label{fig:sweep}
\end{figure}

Figure~\ref{fig:sweep} shows p50 GET latency across value sizes for
YCSB-B. At 64B (6.4\,MB working set, fits in L3), all systems are
cache-resident. At 256B (25\,MB), FurrBallTL holds 100\% hit rate while
CacheLib drops to 95\%. At 1024B (100\,MB vs.\ 32\,MB cache), both are
oversubscribed at $\sim$94\% hit rate, but FurrBallTL's per-node placement
keeps GET at 80\,ns vs.\ CacheLib's 370\,ns ($4.6\times$).

On c6a at 128\,MB working set (1024B values), FurrBallTL achieves
300\,ns GET vs.\ CacheLib's 2,840\,ns---a \textbf{9.5$\times$ advantage} at
\textbf{half the memory footprint} (100\% vs.\ 50\% memory efficiency).

\subsection{YCSB Standard Workloads}

\begin{figure}[t]
\centering
\resizebox{\columnwidth}{!}{\input{figures/ycsb_latency_64b.pgf}}
\caption{p50 GET latency for YCSB workloads A, B, C (64B values, 2 threads).
  FurrBallTL: 80\,ns across all workloads. CacheLib: 880--1,800\,ns.}
\label{fig:ycsb}
\end{figure}

Figure~\ref{fig:ycsb} shows p50 GET across YCSB workloads at 64B.
FurrBallTL achieves 80\,ns on all three---22$\times$ faster than CacheLib
on YCSB-A. FurrBallSN (560\,ns) shows the cost of shared-node contention,
confirming that per-node partitioning, not just NUMA allocation, drives the
benefit.

\begin{figure}[t]
\centering
\resizebox{\columnwidth}{!}{\input{figures/ycsb_tl_vs_cachelib.pgf}}
\caption{FurrBallTL vs.\ CacheLib head-to-head on YCSB A/B/C (64B, 2T).
  Top: latency. Bottom: throughput.}
\label{fig:headtohead}
\end{figure}

Figure~\ref{fig:headtohead} isolates the FurrBallTL vs.\ CacheLib comparison.
FurrBallTL's throughput advantage ranges from $6.6\times$ (YCSB-A) to
$5.5\times$ (YCSB-C). The gap is consistent across read/write ratios,
confirming that the benefit is from placement, not eviction policy.

\subsection{Thread Scaling}

\begin{figure}[t]
\centering
\resizebox{\columnwidth}{!}{\input{figures/thread_scaling_fix.pgf}}
\caption{Thread scaling on c6a (4 NUMA nodes) before and after per-node stat
  fix. Top: before. Bottom: after. Before: FurrBallTL degrades 6.6$\times$ at 4T. After: flat at 71\,ns.}
\label{fig:scaling}
\end{figure}

Figure~\ref{fig:scaling} shows thread scaling on c6a. After the per-node
statistics fix, FurrBallTL scales near-perfectly: 71\,ns at 4T (one thread
per NUMA node) matching 70\,ns at 1T. Each additional node adds an
independent domain with its own CMap, lock, and maintenance thread---no
shared state is touched on the GET path.

CacheLib degrades from 115\,ns (1T) to 337\,ns (4T) due to shared hash
table contention. FurrBallSN degrades from 75\,ns to 453\,ns as multiple
threads contend on a single node's SpinLock. Only FurrBallTL and TBB scale
flatly; TBB because it has no eviction, FurrBallTL because per-node
partitioning eliminates contention.

\subsection{Memory Efficiency}

CacheLib's slab allocator reserves 50\% of allocated memory for metadata,
fragmentation, and internal fragmentation across size classes. This is
structural and vendor-independent (confirmed on both Intel and AMD).
Furrballs uses direct page allocation with a bump allocator: 100\% of
allocated memory is available for key-value data. A 64\,MB FurrBall
cache holds the same working set as a 128\,MB CacheLib cache.

% ========================================================================
\section{Discussion}
\label{sec:discussion}

\textbf{Locality assumption.} Thread-local routing assumes read-after-write
locality: the same node that writes a key will read it. This holds for web
caches, database buffer pools, and CDN edge caches. If the workload violates
this assumption (e.g., writes on node~0, reads on node~1), every TL read
becomes a cross-node access. A NUMA-aware load balancer that distributes
keys based on access frequency is future work.

\textbf{CXL extension.} The architecture generalizes beyond NUMA: CXL-attached
memory tiers (DRAM, PMEM, disaggregated pools) are additional ``nodes'' with
different latency/capacity characteristics. The same per-node page pool and
eviction model applies, with a migration policy for tier-aware promotion.

\textbf{Cache-line alignment caveat.} Our statistics regression demonstrates
that optimizations correct for one topology tier (intra-socket false sharing)
can be actively harmful for another (inter-socket cache-line bouncing).
\texttt{alignas(64)} is not a substitute for NUMA-aware placement of
performance counters.

% ========================================================================
\section{Related Work}
\label{sec:related}

\textbf{NUMA-aware allocation.} \texttt{numactl}, \texttt{libnuma}, and
Windows NUMA APIs provide node-binding primitives, but leave placement
policy to the application. Intel's TBB offers concurrent containers without
NUMA awareness. jemalloc and tcmalloc support NUMA-aware allocation but
are general-purpose allocators, not caches.

\textbf{Production caches.} CacheLib~\cite{cachelib} is Facebook's
production cache, used across their social graph and content delivery
systems. It supports LRU and LRU-2Q policies but uses a shared hash table
with no NUMA binding. RocksDB's block cache uses sharded LRU with no
topology awareness. memcached is single-threaded per core with no NUMA
placement.

\textbf{Topology-aware systems.} FAC (Fabric-Aware Caching)~\cite{fac}
considers NUMA for distributed caches. Caladan~\cite{caladan} uses core
stealing for NUMA-aware scheduling. Shinjuku~\cite{shinjuku} uses
centralized scheduling for latency. None provides per-node page allocation
with independent eviction.

\textbf{ARC.} The Adaptive Replacement Cache~\cite{arc} balances recency
and frequency with a single parameter~$p$. Furrballs uses one ARC instance
per NUMA node, which is simpler than a global ARC with topology-aware
eviction but may under-utilize cold nodes in skewed workloads.

% ========================================================================
\section{Conclusion}
\label{sec:conclusion}

Memory is not uniform, and caches should not treat it as such. Furrballs
demonstrates that per-node page allocation, thread-local key routing, and
per-node lock partitioning yield substantial, measurable benefits on real
NUMA hardware: up to $9.5\times$ lower latency than CacheLib at half the
memory footprint, validated on two vendors (Intel, AMD) and two topologies
(2-node, 4-node).

The per-node statistics regression offers a cautionary lesson:
\emph{NUMA awareness must extend to performance counters, not just data
structures.} The same cache-line isolation that prevents intra-socket false
sharing maximizes inter-socket contention when atomics reside on a single
node.

% ========================================================================
\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
